<form method="post" action="{{ path('commercial_delete', {'comId': commercial.comId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commercial.comId) }}">
    <button class="btn">Delete</button>
</form>
