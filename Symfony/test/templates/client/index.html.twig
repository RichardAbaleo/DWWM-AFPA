{% extends 'base.html.twig' %}

{% block title %}Client index{% endblock %}

{% block body %}
    <h1>Client index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>CliName</th>
                <th>CliBirthdate</th>
                <th>CliPhoneNumber</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for client in clients %}
            <tr>
                <td>{{ client.id }}</td>
                <td>{{ client.cliName }}</td>
                <td>{{ client.cliBirthdate ? client.cliBirthdate|date('Y-m-d') : '' }}</td>
                <td>{{ client.cliPhoneNumber }}</td>
                <td>
                    <a href="{{ path('client_show', {'id': client.id}) }}">show</a> /
                    <a href="{{ path('client_edit', {'id': client.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('client_new') }}">Create new</a>
{% endblock %}
